compile the cpp file with the command :  g++ .\LK.cpp -o LK.exe -std=c++14
# NOTE : must have mingw64 compiler installed
or just run the pre compiled file with the command(in vscode terminal) : .\LK.exe
